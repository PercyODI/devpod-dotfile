return {
  "snacks.nvim",
  opts = {
    -- Set the configuration for the left file explorer window
    explorer = {
      replace_netrw = false,
      trash = false,
    },
    -- Set the notification window settings
    notifier = {
      top_down = false,
    },
  },
}
